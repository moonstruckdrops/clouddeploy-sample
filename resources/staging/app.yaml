runtime: custom
env: flex
service: staging

network:
  name: ${GOOGLE_PROJECT_ID}
  subnetwork_name: ${GOOGLE_PROJECT_ID}
  instance_tag: staging

resources:
  cpu: 2
  memory_gb: 7.6
  disk_size_gb: 10

manual_scaling:
  instances: 1
